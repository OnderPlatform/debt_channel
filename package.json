{
  "name": "@onder/debt-channel",
  "private": true,
  "version": "1.0.0",
  "license": "UNLICENSED",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream",
    "ci:coverage": "lerna run ci:coverage --stream",
    "bootstrap": "lerna bootstrap",
    "doc:html": "sphinx-build -M html docs/ docs/_build",
    "doc:watch": "sphinx-autobuild docs/ docs/_build"
  },
  "devDependencies": {
    "lerna": "^3.6.0",
    "wsrun": "^2.2.0",
    "typescript": "^3.3.1",
    "coveralls": "^3.0.3"
  }
}
